- Name: Do it
  Command: echo "Doing it!"

- Name: build backend
  Parallel: true
  Steps:

    - Name: build foo
      Command: echo "make foo"

    - Name: build bar
      Command: echo "make bar"

    - Name: build baz
      Command: echo "make baz"
